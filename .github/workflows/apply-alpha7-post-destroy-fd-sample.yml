name: Apply alpha.7 post-destroy FD sampling

on:
  push:
    branches:
      - feature/v0.3.0-alpha7

permissions:
  contents: write

concurrency:
  group: apply-alpha7-post-destroy-fd-sample
  cancel-in-progress: false

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/v0.3.0-alpha7
          fetch-depth: 2
      - name: Apply and remove patch files
        shell: bash
        run: |
          set -euo pipefail
          python3 scripts/fix_alpha7_post_destroy_fd_sample.py
          git rm -f \
            scripts/fix_alpha7_post_destroy_fd_sample.py \
            .github/workflows/apply-alpha7-post-destroy-fd-sample.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: sample FD state after VpnService destruction"
          git push origin HEAD:feature/v0.3.0-alpha7
