Publish ConnectX v0.3.0-alpha.3 from the exact main commit by reusing the immutable artifacts from successful Android CI run 31037664649.

The workflow verifies the run status and head SHA, proves that only release infrastructure differs from the target main commit, checks pinned APK/native SHA-256 values and legal payloads, then creates the prerelease and checksum bundle.
