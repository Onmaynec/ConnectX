Publish ConnectX v0.3.0-alpha.4 from the immutable successful Android CI run 31078949278.

The publisher verifies source and final trees directly, allowing only this marker and its workflow to differ. It must also verify CI identity, APK/native SHA-256 values and legal payloads before creating the prerelease.
