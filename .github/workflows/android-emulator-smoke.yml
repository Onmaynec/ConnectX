name: Android Emulator Smoke

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  native-runtime-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Go 1.26.3
        uses: actions/setup-go@v6
        with:
          go-version: "1.26.3"
          cache-dependency-path: |
            engine/go/go.mod
            engine/go/go.sum

      - name: Configure Android runtime paths
        shell: bash
        run: |
          echo "ANDROID_AVD_HOME=$RUNNER_TEMP/connectx-avd" >> "$GITHUB_ENV"
          echo "ANDROID_USER_HOME=$RUNNER_TEMP/connectx-android-user" >> "$GITHUB_ENV"

      - name: Install native and emulator toolchains
        shell: bash
        run: |
          mkdir -p "$ANDROID_AVD_HOME" "$ANDROID_USER_HOME"
          yes | "$ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager" --licenses >/dev/null || true
          "$ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager" \
            "ndk;28.0.13004108" \
            "platform-tools" \
            "emulator" \
            "platforms;android-35" \
            "system-images;android-35;google_apis;x86_64"

      - name: Verify locked Go dependencies
        working-directory: engine/go
        shell: bash
        run: |
          go mod download
          go mod verify
          go test -mod=readonly ./bridge
          git diff --exit-code -- go.mod go.sum

      - name: Build x86_64 Android native bridge
        shell: bash
        run: |
          chmod +x engine/go/build-android.sh
          engine/go/build-android.sh "$ANDROID_SDK_ROOT/ndk/28.0.13004108"
          test -s engine/go/build/android/jniLibs/x86_64/libconnectxbridge.so

      - name: Set up Gradle 8.13
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.13"

      - name: Create deterministic AVD
        shell: bash
        run: |
          test -e /dev/kvm
          sudo chmod 666 /dev/kvm
          "$ANDROID_SDK_ROOT/emulator/emulator" -accel-check | tee accel-check.txt
          echo no | "$ANDROID_SDK_ROOT/cmdline-tools/latest/bin/avdmanager" \
            create avd \
            --force \
            --name connectx-ci \
            --path "$ANDROID_AVD_HOME/connectx-ci.avd" \
            --package "system-images;android-35;google_apis;x86_64"
          "$ANDROID_SDK_ROOT/emulator/emulator" -list-avds | tee avd-list.txt
          grep -qx 'connectx-ci' avd-list.txt

      - name: Start Android emulator
        shell: bash
        run: |
          ADB="$ANDROID_SDK_ROOT/platform-tools/adb"
          EMULATOR="$ANDROID_SDK_ROOT/emulator/emulator"

          nohup "$EMULATOR" \
            -avd connectx-ci \
            -accel on \
            -no-window \
            -noaudio \
            -no-boot-anim \
            -no-snapshot \
            -no-snapshot-save \
            -no-metrics \
            -gpu swiftshader_indirect \
            > emulator.log 2>&1 &
          echo "$!" > emulator.pid

          sleep 5
          kill -0 "$(cat emulator.pid)"
          "$ADB" start-server
          timeout 300 "$ADB" wait-for-device

          DEADLINE=$((SECONDS + 300))
          until [[ "$("$ADB" shell getprop sys.boot_completed 2>/dev/null | tr -d '\r')" == "1" ]]; do
            if ! kill -0 "$(cat emulator.pid)" 2>/dev/null; then
              cat accel-check.txt 2>/dev/null || true
              cat avd-list.txt 2>/dev/null || true
              cat emulator.log
              exit 1
            fi
            if (( SECONDS >= DEADLINE )); then
              cat emulator.log
              exit 1
            fi
            sleep 5
          done

          "$ADB" shell getprop ro.product.cpu.abi
          "$ADB" shell getprop ro.build.version.sdk

      - name: Run JNI instrumentation smoke test
        run: gradle --no-daemon :vpn:nativebridge:connectedDebugAndroidTest

      - name: Collect diagnostics
        if: always()
        shell: bash
        run: |
          ADB="$ANDROID_SDK_ROOT/platform-tools/adb"
          mkdir -p emulator-diagnostics
          cp emulator.log emulator-diagnostics/ 2>/dev/null || true
          cp emulator.pid emulator-diagnostics/ 2>/dev/null || true
          cp accel-check.txt emulator-diagnostics/ 2>/dev/null || true
          cp avd-list.txt emulator-diagnostics/ 2>/dev/null || true
          timeout 10 "$ADB" devices -l > emulator-diagnostics/devices.txt 2>&1 || true
          timeout 10 "$ADB" logcat -d > emulator-diagnostics/logcat.txt 2>&1 || true
          cp -R vpn/nativebridge/build/reports/androidTests emulator-diagnostics/reports 2>/dev/null || true
          cp -R vpn/nativebridge/build/outputs/androidTest-results emulator-diagnostics/results 2>/dev/null || true

      - name: Upload diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: connectx-android-emulator-${{ github.sha }}
          path: emulator-diagnostics
          if-no-files-found: warn
          retention-days: 14
